This project is titled "no-db" because it was built without a database;data is stored temporarily in arrays on the server. It runs on Express server and front end was built with React.

The app maks a call to an external api and renders its content on the screen